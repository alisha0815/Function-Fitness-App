<div class="container">

  <div class="row mb-3">
    <div class="col-12">
      <h2 class="text-center"> <%= @personalized_trainer.name %> is a personal trainer in <%= @personalized_trainer.category %> </h2>
    </div>
  </div>

  <div class="row">
    <div class="col-7 shadow m-2 p-3">
      <div class="card-trip">
       <% if @personalized_trainer.photo.attached? %>
        <%= cl_image_tag @personalized_trainer.photo.key, height: 300 , width: 200, crop: :fill%>
        <% end %>
        <div class="card-trip-infos">
          <h4><%= @personalized_trainer.name %></h4>
          <h5> Category: <%= @personalized_trainer.category %></h5>
          <p> <i class="fas fa-globe-africa theme-color"></i> <%= @personalized_trainer.location %></p>
          <p> <%= @personalized_trainer.description %></p>
          <p> € <%= @personalized_trainer.rate %></p>
        </div>

<div class="col-4 shadow m-2 p-3">
  <div id='personalized_trainer-booking-dates' data-unavailable="<%= @personalized_trainer.unavailable_dates.to_json %>">
    <h5> BOOK PT </h5>
    <% if current_user.id != @personalized_trainer.user_id  %>
       <%= simple_form_for([@personalized_trainer, @booking]) do |f| %>
         <div class="form-inputs">
           <div class="form-row">
             <%= f.label :start_date, 'Start Date' %>
             <%= f.input :start_date, placeholder: "To", as: :string, type: :text, label: false %>
             <%= f.label :end_date, 'End Date' %>
             <%= f.input :end_date, placeholder: "To", as: :string, type: :text, label: false %>
             <%= f.input :goal, class: "btn btn-primary btn-lg btn-block btn-booking" %>
            </div>
          </div>
<<<<<<< HEAD
          
          <div>
            <table class="table">
              <tr>
                <td>NOK<%= @personalized_trainer.rate %> x <%#= (@booking.end_date - @booking.start_date).to_i %> days</td>
                <td class="text-right"></td>
              </tr>
              <tr>
                <td><strong>Total</strong></td>
                <td class="text-right">NOK <%#= @booking.fee %></td>
              </tr>
            </table>
          </div>
          
          <div class="form-inputs">
            <%= f.submit "Book", class: "btn-theme btn-block btn-booking", data: { confirm: "Are you sure you want to confirm your booking?" } %>
            <%= link_to "Browse", personalized_trainers_path, class: "btn-theme-cancel btn-block text-center" %>
          </div>
=======
        </div>

        <div class="form-inputs">
          <%= f.input :goal, class: "btn btn-primary btn-lg btn-block btn-booking" %>
          <%= f.submit "Book now", class: "btn-theme btn-block btn-booking" %>
          <%= link_to "Browse", personalized_trainers_path, class: "btn-theme-cancel btn-block text-center" %>
        </div>
>>>>>>> master
        <% end %>
    <% end %>
 </div>
</div>







  <div class="row">
    <div class="col-11 shadow m-3 p-4">
      <h3>Reviews</h3>

      <% if @bookings.empty? %>
        <small><em class="text-muted">Be the first one to review <%= @personalized_trainer.name %> </em></small>
      <% end %>

      <% if @bookings %>
        <% @bookings.each do |booking| %>
          <% booking.review.each do |review| %>
          <div class="border-bottom mt-3">
            <div>
              <% review.rating.times do %>
                <i class="fas fa-star star-yellow theme-color"></i>
              <% end %>
              <small><em class="text-muted"><%= distance_of_time_in_words_to_now(review.created_at) %> ago</em></small>
              <p class="mb-0"><%= review.comment %></p>
            </div>
            <small><em class="text-muted"> <%= booking.user.name %> </em></small>
          </div>
          <% end %>
        <% end %>
      <% end %>

    </div>
  </div>

</div>
